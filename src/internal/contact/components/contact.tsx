import React from 'react';
import ContactMDX from '../documents/contact.mdx';
import styled from '@emotion/styled';

const Theme = styled.div``;

export const Contact = () => (
	<Theme>
		<ContactMDX />
	</Theme>
);

export default Contact;
